{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "parcel index.html --no-cache",
    "build": "parcel build index.html --out-dir ../docs --no-minify --no-cache"
  },
  "dependencies": {
    "simple-scroll-animate": "^0.2.0"
  },
  "alias": {},
  "devDependencies": {
    "parcel": "^1.12.3",
    "typescript": "^3.4.5"
  }
}